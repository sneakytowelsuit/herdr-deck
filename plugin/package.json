{
  "name": "herdr-deck-plugin",
  "version": "0.1.0",
  "description": "Stream Deck plugin frontend for herdr-deck (macOS)",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist-test/*.test.js",
    "pretest": "tsc -p tsconfig.test.json"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.0",
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@rollup/plugin-typescript": "^12.1.0",
    "@types/node": "^22.0.0",
    "rollup": "^4.20.0",
    "tslib": "^2.7.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.1.0"
  }
}
